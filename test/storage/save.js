// Tests saving data to files
// jshint mocha:true
'use strict';

var expect = require('chai').expect;
var config = require('../../index.js');

describe('Storing layer data to files', function() {

    describe('saveToFile()', function() {
        it('save the data of one layer into a file');
    });

    describe('saveToDirectory()', function() {
        it('saving the data of all layers into a directory');
        it('ignore flagged layers when saving to directory');
    });
});